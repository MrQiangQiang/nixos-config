# ripgrep — recursive search (Layer 0: ANSI auto-follows terminal palette)
# No theme config — uses ANSI colors that follow foot's palette (darkman-managed).
# Defaults left at ripgrep's sensible built-ins; AI tools pass explicit flags.
{ programs.ripgrep.enable = true; }
