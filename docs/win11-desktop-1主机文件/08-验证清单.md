# 08 验证清单

> 迁移完成后逐项验证。

## 系统

```bash
nixos-version                              # 26.11
nixos-rebuild list-generations             # 列出所有 generation
nix flake metadata ~/nixos-config          # flake.lock 信息
sudo nixos-rebuild build --flake ~/nixos-config  # 空构建通过
```

## 文件系统

```bash
btrfs subvolume list /                     # 7 个子卷: @ @home @nix @var_cache @var_log @ollama @data_cold
findmnt / -o OPTIONS                       # compress=zstd
swapon --show                              # zram0 (compressed RAM swap)
df -h                                      # / 和 /home 共享 2TB
```

## 开发工具

```bash
fish --version && git --version && python3 --version && node --version
docker --version && ollama --version
mise --version && chezmoi --version
ghostty --version && nix --version
home-manager --version
```

## 网络

```bash
# clash-verge-rev 启动 → 导入订阅 → 开启代理
ping -c 3 baidu.com
curl -I https://www.google.com
```

## 中文输入

```bash
fcitx5-diagnose                           # 无错误
# Ctrl+Space 切换 → 输入中文
```

## 日常软件

| 软件 | 验证 |
|------|------|
| 微信 | 启动、扫码登录、发消息 |
| Brave | 启动、Shields 生效 |
| Chrome | 启动 |
| Telegram | 启动、登录 |
| Obsidian | 启动、打开 vault |
| 百度网盘 | 启动 |
| 向日葵 | 启动、远程连接 |

## chezmoi

```bash
chezmoi doctor                             # 全部通过
chezmoi diff                               # 无差异
```

## 回滚

```bash
sudo nixos-rebuild switch --flake ~/nixos-config#desktop-1
sudo nixos-rebuild list-generations
sudo nixos-rebuild switch --rollback       # 回到上一个
```

## GC

```bash
sudo nix-collect-garbage --dry-run         # 预览
sudo nix-collect-garbage -d                # 执行
sudo nixos-rebuild boot                    # 重建 boot 项
```
